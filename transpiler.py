from runtime import *

# O seu transpilador vai gerar a lógica abaixo dinamicamente:
temperatura = 40  # Inicializado por um comando 'set' ou por padrão como 0
if temperatura > 30:
    ligar("ventilador")